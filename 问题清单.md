# 问题清单

更新时间：2026-06-08

## 验收结论

- Week 01-26 文档配对完整：每周均有 1 个 `.plan.md` 和 1 个 `week_review.md`。
- `week_review.md` 均已标记为 `已稳固` / `已验收` / `Solidified`，且 review 文档中没有未勾选任务。
- 本次重新执行最终验收命令，MVP Release `0.1.0` 功能验收通过。
- 仍存在若干固化问题和增强目标 Deferred 项；这些不阻塞 direct backend MVP 发布，但需要记录。

## 本次验证记录

| 验证项 | 命令 | 结果 |
|---|---|---|
| Release build | `dotnet build src\CSharpAiCli.sln -c Release` | 通过，0 warning，0 error |
| Release tests | `dotnet test src\CSharpAiCli.sln -c Release --no-build` | 通过，250 passed，0 failed，0 skipped |
| Release package | `powershell -NoProfile -ExecutionPolicy Bypass -File tools/Build-Release.ps1` | 通过，生成 release directory 和 zip |
| Smoke tests | `powershell -NoProfile -ExecutionPolicy Bypass -File tools/Invoke-SmokeTests.ps1` | 通过，输出 `smoke tests passed` |
| Release version | `artifacts\release\caicli-0.1.0-win-x64\caicli.exe version` | 输出 `caicli 0.1.0`、`target framework: net9.0`、`release runtime: win-x64` |

当前重新生成的发布 zip：

- Path：`artifacts/release/caicli-0.1.0-win-x64.zip`
- Size：`32108447` bytes
- SHA256：`44B96A18C99343EADEFE7448FB50BB5BCDA0A6E9CD2D384A7F7751A2AE1EF4D2`
- Manifest `builtFromVersion`：`0.1.0`

## 需要处理的问题

### P1 - 发布验收记录与当前重新生成产物不一致，且 zip 非确定性

状态：已修复

处理结果：

- `tools/Build-Release.ps1` 不再写入动态 `createdAtUtc`，manifest 使用稳定字段 `builtFromVersion`。
- 发布 zip 改为 .NET `ZipArchive` 生成，按 entry name 排序并固定 entry `LastWriteTime`。
- 连续两次运行 `tools/Build-Release.ps1` 后，zip size / SHA256 均一致：
  - Size：`32108447` bytes
  - SHA256：`44B96A18C99343EADEFE7448FB50BB5BCDA0A6E9CD2D384A7F7751A2AE1EF4D2`
- 已更新 `docs_md/weekly/26_week_review.md`、`docs_md/release/final_acceptance.md` 和 `docs_md/weekly/26_week_final_acceptance_release.plan.md` 的发布 hash 记录。

影响：

- 不影响当前功能验收。
- 发布审计 hash 已与当前可重复生成产物一致。

建议：

- 后续版本若需要记录构建时间，应使用版本元数据中的固定时间戳或显式参数，避免默认使用当前时间。

### P2 - Week 02-07 plan 文档仍残留未勾选任务

状态：已修复

处理结果：

- Week 02-07 plan 已作为历史实施计划固化，文件顶部补充最终证据指向对应 `week_review.md`。
- 已将六个 plan 文件中残留的 `- [ ]` 任务标记闭合为 `- [x]`。
- 验证命令 `rg -c "^- \[ \]" docs_md/weekly -g "0[2-7]_week*.plan.md"` 无输出，exit code 为 1，表示 Week 02-07 plan 文件中没有匹配的未勾选任务。
- 对应回顾文件 `02_week_review.md` 至 `07_week_review.md` 均存在。

判断：

- 这些 plan 多数是 TDD 执行记录中的历史步骤，代码和 review 验收已通过。
- 文档固化后，plan 文件不再显示未闭合任务。

建议：

- 后续历史 plan 若已由对应 review 验收，可同步闭合 checkbox 或在顶部注明最终状态来源。

### P2 - 仓库 .NET SDK 锁定

状态：已修复

现象：

- 当前保持 `net9.0`。
- 根目录已提交 `global.json`，锁定 `.NET SDK 9.0.308`，并使用 `latestPatch` roll-forward 策略。

影响：

- 不影响本机本次验收。
- 后续在不同机器构建时会优先使用仓库锁定的 SDK 策略，降低 SDK 自动升级带来的构建差异。

处理结果：

- 已创建根目录 `global.json`。
- 已添加测试校验 `global.json` 的 `sdk.version` 为 `9.0.308`，`rollForward` 为 `latestPatch`。
- 已更新当前 release/spec 文档和 26 周排期摘要，不再描述仓库缺少 SDK 锁。

### P3 - 增强目标仍为 Deferred

状态：已记录，不阻塞 MVP

范围：

- 真实 Microsoft Agent Framework runtime backend 未启用。
- 真实 MCP protocol handshake / dynamic tool discovery 未启用。
- Gerber/TIFF 真实转换执行未启用。
- dotnet tool package 不属于首版发布包。

判断：

- 这些项已在 `docs_md/release/capability_status.md`、`docs_md/release/known_limitations.md` 和 `docs_md/release/final_acceptance.md` 中明确标注。
- 不阻塞 direct backend MVP `0.1.0` 发布。

建议：

- 后续如进入增强阶段，应拆分为独立里程碑，不再混入 MVP 验收口径。

## 当前阻塞状态

- 阻塞 MVP 发布的问题：无。
- 阻塞文档固化的问题：无；剩余项为增强目标 Deferred 记录。
