# OpenAI/Codex SSO 自建身份系统开发计划

## 1. 目标

部署一个自有域名的 SSO 身份系统，用于让用户通过域名邮箱登录 OpenAI / ChatGPT / Codex workspace，并默认加入为 Codex seat。

目标形态：

```text
sso.yourdomain.com
  -> authentik
  -> OpenAI Custom OIDC SSO
  -> user@yourdomain.com
  -> Codex seat
```

第一版不自研 OIDC，不仿站点页面，优先使用成熟开源项目 `authentik`，最终通过 Docker Compose 部署到自己的服务器。

## 2. 推荐方案

选型：

```text
身份系统：authentik
部署方式：Docker Compose
反向代理：Caddy
数据库：PostgreSQL
缓存：Redis
证书：Let's Encrypt 自动 HTTPS
OpenAI 接入：Custom OIDC
```

选择 authentik 的原因：

- 自带用户管理、组管理、OIDC Provider、登录页面和后台 UI。
- 比 Keycloak 轻，适合个人或小团队先跑通。
- 比 Dex 更适合没有现成上游账号系统的场景。
- 比自研 OIDC 安全、稳定、省维护。

## 3. 前置资源

需要准备：

```text
域名：yourdomain.com
SSO 子域名：sso.yourdomain.com
服务器：Ubuntu 22.04 / 24.04
最低配置：2 CPU / 2 GB RAM
Docker：Docker Engine + Docker Compose v2
邮箱服务：Google Workspace / Microsoft 365 / Zoho / Cloudflare Email Routing 任选
OpenAI 权限：workspace 管理员或 Global Admin
```

DNS 计划：

```text
sso.yourdomain.com  A      你的服务器公网 IP
yourdomain.com      TXT    OpenAI 域名验证记录
yourdomain.com      MX     域名邮箱服务记录
```

## 4. 阶段 1：服务器初始化

目标：服务器具备 Docker 部署能力，并只开放必要端口。

步骤：

```bash
apt update
apt install -y ca-certificates curl gnupg ufw
```

安装 Docker 和 Docker Compose v2，建议使用 Docker 官方源。

开放端口：

```bash
ufw allow 22/tcp
ufw allow 80/tcp
ufw allow 443/tcp
ufw enable
```

目录规划：

```text
/opt/authentik/
  compose.yml
  .env
  Caddyfile
  backups/
```

验收标准：

- `docker version` 正常。
- `docker compose version` 正常。
- 服务器 80/443 可访问。
- `sso.yourdomain.com` 已解析到服务器 IP。

## 5. 阶段 2：部署 authentik

目标：通过 Docker Compose 启动 authentik、PostgreSQL、Redis 和 Caddy。

部署服务：

```text
authentik-server
authentik-worker
postgresql
redis
caddy
```

环境变量重点：

```text
AUTHENTIK_SECRET_KEY：随机强密钥
AUTHENTIK_POSTGRESQL__PASSWORD：数据库密码
AUTHENTIK_EMAIL__HOST：SMTP 地址
AUTHENTIK_EMAIL__USERNAME：SMTP 用户名
AUTHENTIK_EMAIL__PASSWORD：SMTP 密码
AUTHENTIK_EMAIL__FROM：noreply@yourdomain.com
```

上线命令：

```bash
cd /opt/authentik
docker compose pull
docker compose up -d
docker compose ps
```

验收标准：

- `https://sso.yourdomain.com` 能打开。
- HTTPS 证书正常。
- authentik 初始化页面能访问。
- `authentik-server`、`authentik-worker`、`postgresql`、`redis` 均为 running 状态。

## 6. 阶段 3：初始化身份系统

目标：完成 authentik 的基础身份配置。

配置内容：

```text
1. 初始化 akadmin 管理员。
2. 设置管理员强密码并启用 MFA。
3. 配置 SMTP 发信。
4. 创建用户组：openai-codex-users。
5. 创建测试用户：admin@yourdomain.com、test@yourdomain.com。
6. 确认用户 email 字段使用 yourdomain.com。
7. 确认测试用户能登录 authentik。
```

用户原则：

- OpenAI SSO 使用的邮箱必须是 `user@yourdomain.com`。
- 不要使用临时邮箱或无法收信的邮箱作为管理员邮箱。
- 至少保留一个 OpenAI 管理员账号用于紧急恢复。

验收标准：

- 测试用户能登录 `https://sso.yourdomain.com`。
- 测试用户的 email 字段正确。
- SMTP 测试邮件能收到。

## 7. 阶段 4：创建 OpenAI OIDC Provider

目标：让 authentik 对 OpenAI 暴露标准 OIDC 服务。

在 authentik 中创建 OAuth2 / OpenID Connect Provider：

```text
Name：OpenAI Codex SSO
Authorization flow：default-provider-authorization-explicit-consent
Client type：Confidential
Redirect URI：使用 OpenAI 后台提供的回调地址
Signing key：默认 RS256 签名密钥
Scopes：openid profile email
Subject mode：基于用户唯一 ID
Access restriction：openai-codex-users
```

需要记录：

```text
Discovery URL
Client ID
Client Secret
```

Discovery URL 形态通常类似：

```text
https://sso.yourdomain.com/application/o/openai-codex-sso/.well-known/openid-configuration
```

必须确认返回的 claims 包含：

```json
{
  "sub": "stable-user-id",
  "name": "User Name",
  "email": "user@yourdomain.com",
  "email_verified": true
}
```

验收标准：

- Discovery URL 返回 JSON。
- JWKS endpoint 可访问。
- OIDC scopes 包含 `openid`、`profile`、`email`。
- 测试用户的 `email` claim 是域名邮箱。

## 8. 阶段 5：配置 OpenAI 域名验证

目标：让 OpenAI 确认 `yourdomain.com` 归你管理。

OpenAI 后台路径：

```text
admin.openai.com/identity
  -> Verified Domains
  -> Add Domain
```

操作：

```text
1. 输入 yourdomain.com。
2. 复制 OpenAI 给出的 DNS TXT 记录。
3. 到域名 DNS 服务商添加 TXT。
4. 等待 DNS 生效。
5. 回到 OpenAI 后台点击 Check。
```

验收标准：

- OpenAI 后台显示 `yourdomain.com` 已验证。
- DNS TXT 查询能看到 OpenAI 验证记录。

## 9. 阶段 6：配置 OpenAI Custom OIDC SSO

目标：把 OpenAI workspace 的登录交给 authentik。

OpenAI 后台路径：

```text
admin.openai.com/identity
  -> Set up SSO
  -> Custom OIDC
```

填写：

```text
Discovery URL：authentik 的 discovery URL
Client ID：authentik 中生成的 client ID
Client Secret：authentik 中生成的 client secret
```

上线策略：

```text
第一步：SSO Optional
第二步：无痕窗口测试登录
第三步：测试管理员仍可进入后台
第四步：测试普通用户能进入 workspace
第五步：确认无误后再考虑 Required
```

验收标准：

- OpenAI OIDC 测试通过。
- 用户通过 `user@yourdomain.com` 登录成功。
- OpenAI 收到的邮箱与 authentik 用户 email 一致。
- 管理员没有被 SSO 锁出后台。

## 10. 阶段 7：配置 Codex seat

目标：避免新用户通过 SSO 加入后变成标准 ChatGPT seat。

OpenAI 后台路径：

```text
Workspace settings
  -> Identity & access
  -> User provisioning
  -> Default seat type
  -> Codex
```

邀请用户时使用：

```csv
email,role,seat type
alice@yourdomain.com,member,Codex
bob@yourdomain.com,member,Codex
```

验收标准：

- 新用户加入后 seat type 是 Codex。
- 已有用户可在 Members 页面手动调整 seat type。
- 不需要 ChatGPT 的用户不占用标准 ChatGPT seat。

## 11. 阶段 8：完整测试清单

上线前逐项确认：

```text
[ ] sso.yourdomain.com DNS 正确。
[ ] HTTPS 证书正常。
[ ] authentik 管理员可登录。
[ ] 普通用户可登录。
[ ] SMTP 可发信。
[ ] OpenAI 域名验证成功。
[ ] OIDC Discovery URL 可访问。
[ ] JWKS endpoint 可访问。
[ ] OIDC token 使用 RS256 签名。
[ ] userinfo 返回 email。
[ ] email_verified 为 true。
[ ] OpenAI Custom OIDC 测试通过。
[ ] OpenAI SSO 先保持 Optional。
[ ] 无痕窗口测试成功。
[ ] 管理员紧急登录路径可用。
[ ] 默认 seat type 已设置为 Codex。
[ ] 测试用户加入后是 Codex seat。
```

## 12. 阶段 9：备份与运维

备份内容：

```text
/opt/authentik/.env
/opt/authentik/compose.yml
/opt/authentik/Caddyfile
PostgreSQL 数据库
authentik media / custom templates
```

每日数据库备份示例：

```bash
mkdir -p /opt/authentik/backups
docker compose exec -T postgresql pg_dump -U authentik authentik > /opt/authentik/backups/authentik-$(date +%F).sql
```

升级流程：

```bash
cd /opt/authentik
docker compose pull
docker compose up -d
docker compose ps
```

恢复演练：

```text
1. 新建临时服务器。
2. 恢复 .env、compose.yml、Caddyfile。
3. 启动 PostgreSQL。
4. 导入最近一次数据库备份。
5. 启动 authentik。
6. 验证用户、OIDC Provider、OpenAI 登录仍正常。
```

## 13. 时间安排

建议 5 天完成第一版：

```text
第 1 天：服务器、DNS、Docker、Caddy、HTTPS。
第 2 天：部署 authentik，初始化管理员、SMTP、用户组。
第 3 天：创建 OIDC Provider，测试 discovery、JWKS、claims。
第 4 天：OpenAI 域名验证、Custom OIDC、SSO Optional 测试。
第 5 天：Codex seat、用户邀请、备份、恢复演练、上线文档。
```

## 14. 风险与规避

风险 1：SSO 配错导致管理员无法登录。

规避：

```text
先使用 Optional。
保留至少一个可恢复的管理员账号。
用无痕窗口测试，不要直接切 Required。
```

风险 2：email claim 不正确。

规避：

```text
上线前检查 userinfo。
确保 authentik 用户 email 是 user@yourdomain.com。
不要用 display name 替代 email。
```

风险 3：域名邮箱和 SSO 混淆。

规避：

```text
authentik 负责身份登录。
邮箱服务负责真实收发邮件。
OpenAI 域名验证负责证明域名所有权。
三者分别配置，不要混成一个步骤。
```

风险 4：Docker 数据丢失。

规避：

```text
PostgreSQL 使用持久化 volume。
每天备份数据库。
升级前先备份。
每月至少做一次恢复演练。
```

## 15. 后续版本

第一版跑通后，再考虑：

```text
1. 自定义登录页面品牌。
2. 接入 GitHub / Google / Microsoft 作为上游登录源。
3. 做用户自助申请页面。
4. 做管理员审核流程。
5. 做自动化用户同步。
6. 从 authentik 迁移到 Dex / OpenIddict / 自研 OIDC 桥接器。
```

## 16. 参考资料

- authentik 官方文档：https://docs.goauthentik.io/
- authentik Docker Compose 部署：https://docs.goauthentik.io/install-config/install/docker-compose/
- authentik OAuth2 / OIDC Provider：https://docs.goauthentik.io/add-secure-apps/providers/oauth2/
- OpenAI 域名验证：https://help.openai.com/en/articles/8871611-domain-verification
- OpenAI SSO 配置：https://help.openai.com/en/articles/10472980-enabling-sso-on-chatgpt
